{
  "name": "dashboard-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check src/config.js && node --check src/googleOAuth.js && node --check src/tokenStore.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "mongodb": "^7.5.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
