name: dashboard
hostname: dashboard.whodrey.com
type: docker
container_port: 80

# private   = deploy only, no public route
# public    = publicly reachable
# protected = public hostname, but requires Cloudflare Access login
access: protected
