services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: ${APP_NAME:-homelab-app}
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "${HOST_PORT}:80"